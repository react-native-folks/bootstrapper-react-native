import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  defaultSize: {
    height: 150,
    width: 150
  },
  container: {
    position: 'absolute',
    bottom: 0,
    top: 0,
    left: 0,
    right: 0
  }
});
